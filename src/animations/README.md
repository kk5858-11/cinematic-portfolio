# animations/

独立动画层。禁止在 `components/sections` 内直接写 GSAP timeline 或复杂 Framer 变体。

- `framer/` — 组件级入场、stagger、layout 共享
- `gsap/` — ScrollTrigger、pin、scrub、Hero 电影时间轴
- `transitions/` — App Router 路由转场壳
- `presets/` — 与 `tokens/motion` 对齐的预设
